# Infra Repository

This repository contains Terraform configuration for deploying infrastructure.

## Usage

Initialize Terraform:

```bash
terraform init
```

Apply the infrastructure:


```bash
terraform apply
```

Destroy the infrastructure (optional):

```bash
terraform destroy
```
